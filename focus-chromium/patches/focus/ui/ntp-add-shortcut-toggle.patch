diff --git a/chrome/browser/new_tab_page/ntp_pref_names.h b/chrome/browser/new_tab_page/ntp_pref_names.h
index 5506efb960013d67c9dcb80dfd0b34ba3d9cc9c6..203ba61f06dd605eefd650affe9fba2de1824ea3 100644
--- a/chrome/browser/new_tab_page/ntp_pref_names.h
+++ b/chrome/browser/new_tab_page/ntp_pref_names.h
@@ -10,6 +10,11 @@ namespace ntp_prefs {
 // Tracks whether the user has chosen to hide the shortcuts tiles on the NTP.
 inline constexpr char kNtpShortcutsVisible[] = "ntp.shortcust_visible";
 
+// Tracks whether the user has chosen to show the add-shortcut tile while
+// keeping the rest of the shortcut section visible.
+inline constexpr char kNtpAddShortcutVisible[] =
+    "ntp.add_shortcut_visible";
+
 // Tracks whether the user has chosen to use custom links or most visited sites
 // for the shortcut tiles on the NTP. This pref is migrated to
 // `kNtpShortcutsType`.
diff --git a/chrome/browser/resources/side_panel/customize_chrome/shortcuts.html.ts b/chrome/browser/resources/side_panel/customize_chrome/shortcuts.html.ts
index 44a36b99410b45713fe3bcd33148a2a5c5647301..1bc81fb59b32164c514add01200419a6c7fee467 100644
--- a/chrome/browser/resources/side_panel/customize_chrome/shortcuts.html.ts
+++ b/chrome/browser/resources/side_panel/customize_chrome/shortcuts.html.ts
@@ -17,6 +17,16 @@ export function getHtml(this: ShortcutsElement) {
 </div>
 <div id="options">
   <cr-collapse ?opened="${this.show_}" ?no-animation="${!this.initialized_}">
+    <hr class="sp-hr">
+    <div id="addShortcutContainer" class="option"
+        @click="${this.onShowAddShortcutClick_}">
+      <customize-chrome-button-label label="$i18n{addShortcutTitle}">
+      </customize-chrome-button-label>
+      <cr-toggle id="addShortcutToggle" title="$i18n{addShortcutTitle}"
+          ?checked="${this.showAddShortcut_}"
+          @change="${this.onShowAddShortcutChange_}">
+      </cr-toggle>
+    </div>
     <hr class="sp-hr">
     <div id="enterpriseShortcutsMixedContainer" class="option" @click="${
       this.onShowEnterpriseShortcutsClick_}" ?hidden="${
diff --git a/chrome/browser/resources/side_panel/customize_chrome/shortcuts.ts b/chrome/browser/resources/side_panel/customize_chrome/shortcuts.ts
index cf22922ed421daec6ffec26d21f2c8d69477cc12..28d408e2672258f8525c260445c6feb510fc6c5e 100644
--- a/chrome/browser/resources/side_panel/customize_chrome/shortcuts.ts
+++ b/chrome/browser/resources/side_panel/customize_chrome/shortcuts.ts
@@ -27,6 +27,7 @@ export interface ShortcutsElement {
   $: {
     showToggleContainer: HTMLElement,
     showToggle: CrToggleElement,
+    addShortcutToggle: CrToggleElement,
     radioSelection: CrRadioGroupElement,
   };
 }
@@ -59,6 +60,7 @@ export class ShortcutsElement extends CrLitElement {
       initialized_: {type: Boolean},
       radioSelection_: {type: String},
       show_: {type: Boolean},
+      showAddShortcut_: {type: Boolean},
       showPersonalShortcuts_: {type: Boolean},
       showEnterpriseShortcuts_: {type: Boolean},
       shortcutConfigs_: {type: Array},
@@ -70,6 +72,7 @@ export class ShortcutsElement extends CrLitElement {
   protected accessor initialized_: boolean = false;
   protected accessor radioSelection_: string|undefined = undefined;
   protected accessor show_: boolean = false;
+  protected accessor showAddShortcut_: boolean = true;
   protected accessor showPersonalShortcuts_: boolean = false;
   protected accessor showEnterpriseShortcuts_: boolean = false;
   protected accessor shortcutConfigs_: ShortcutConfig[] = [];
@@ -86,11 +89,13 @@ export class ShortcutsElement extends CrLitElement {
         this.apiProxy_.callbackRouter.setMostVisitedSettings.addListener(
             (shortcutsTypes: TileType[], shortcutsVisible: boolean,
              shortcutsPersonalVisible: boolean,
+             addShortcutVisible: boolean,
              disabledShortcuts: TileType[]) => {
               // Only track personal shortcut types in `shortcutsType_`.
               this.shortcutsType_ =
                   shortcutsTypes.find(t => t !== TileType.kEnterpriseShortcuts);
               this.show_ = shortcutsVisible;
+              this.showAddShortcut_ = addShortcutVisible;
               this.disabledShortcuts_ = disabledShortcuts;
               this.showPersonalShortcuts_ = shortcutsPersonalVisible;
               this.showEnterpriseShortcuts_ =
@@ -186,6 +191,22 @@ export class ShortcutsElement extends CrLitElement {
     this.setShow_(!this.show_);
   }
 
+  private setShowAddShortcut_(show: boolean) {
+    if (this.showAddShortcut_ === show) {
+      return;
+    }
+    this.showAddShortcut_ = show;
+    this.apiProxy_.handler.setAddShortcutVisible(show);
+  }
+
+  protected onShowAddShortcutChange_(e: CustomEvent<boolean>) {
+    this.setShowAddShortcut_(e.detail);
+  }
+
+  protected onShowAddShortcutClick_() {
+    this.setShowAddShortcut_(!this.showAddShortcut_);
+  }
+
   private setShowPersonalShortcuts_(show: boolean) {
     if (this.showPersonalShortcuts_ === show) {
       return;
diff --git a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.cc b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.cc
index 52976149766bb0e936a160d20258eda3e85f8774..a73f7b141096d1cd72b10b26107a8acb6118d22d 100644
--- a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.cc
+++ b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.cc
@@ -91,6 +91,12 @@ MostVisitedHandler::MostVisitedHandler(
 
 // TODO(b/502297163): Implement for Android.
 #if !BUILDFLAG(IS_ANDROID)
+  pref_change_registrar_.Init(profile_->GetPrefs());
+  pref_change_registrar_.Add(
+      ntp_prefs::kNtpAddShortcutVisible,
+      base::BindRepeating(&MostVisitedHandler::UpdateMostVisitedInfo,
+                          weak_ptr_factory_.GetWeakPtr()));
+
   web_app::WebAppProvider* web_app_provider_ =
       web_app::WebAppProvider::GetForWebApps(profile);
   if (web_app_provider_) {
@@ -433,6 +439,12 @@ void MostVisitedHandler::OnURLsAvailable(
   }
   result->tiles = std::move(tiles);
   result->custom_links_enabled = most_visited_sites_->IsCustomLinksEnabled();
+#if BUILDFLAG(IS_ANDROID)
+  result->add_shortcut_visible = true;
+#else
+  result->add_shortcut_visible =
+      profile_->GetPrefs()->GetBoolean(ntp_prefs::kNtpAddShortcutVisible);
+#endif
   result->enterprise_shortcuts_enabled =
       most_visited_sites_->IsEnterpriseShortcutsEnabled();
 #if BUILDFLAG(IS_ANDROID)
diff --git a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.h b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.h
index 39237a3459d2785dbb78744a042b951bbf77c320..be6e6fc704698d6dc5599076077f8aaebf9c0551 100644
--- a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.h
+++ b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler.h
@@ -24,6 +24,7 @@
 #include "url/gurl.h"
 #else
 #include "chrome/browser/web_applications/preinstalled_web_app_manager.h"
+#include "components/prefs/pref_change_registrar.h"
 #endif
 
 class GURL;
@@ -142,6 +143,7 @@ class MostVisitedHandler : public most_visited::mojom::MostVisitedPageHandler,
   base::ScopedObservation<web_app::PreinstalledWebAppManager,
                           web_app::PreinstalledWebAppManager::Observer>
       preinstalled_web_app_observer_{this};
+  PrefChangeRegistrar pref_change_registrar_;
 #endif
 
   base::WeakPtrFactory<MostVisitedHandler> weak_ptr_factory_{this};
diff --git a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler_unittest.cc b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler_unittest.cc
index 238855c64248aadbae3aecadb06d6e1cdcbc9ab3..d8c64989fc49f22f9fa7f01db4c5a34791c0d185 100644
--- a/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler_unittest.cc
+++ b/chrome/browser/ui/webui/cr_components/most_visited/most_visited_handler_unittest.cc
@@ -115,6 +115,26 @@ TEST_P(MostVisitedAutoRemovalTest, AddMostVisitedTile) {
       ntp_prefs::kNtpShortcutsAutoRemovalDisabled));
 }
 
+TEST_P(MostVisitedAutoRemovalTest, AddShortcutVisibilityIsSentToPage) {
+  profile_.GetPrefs()->SetBoolean(ntp_prefs::kNtpAddShortcutVisible, false);
+
+  base::RunLoop run_loop;
+  bool received_info = false;
+  EXPECT_CALL(page_, SetMostVisitedInfo)
+      .Times(testing::AtLeast(1))
+      .WillRepeatedly([&](most_visited::mojom::MostVisitedInfoPtr info) {
+        EXPECT_FALSE(info->add_shortcut_visible);
+        if (!received_info) {
+          received_info = true;
+          run_loop.Quit();
+        }
+      });
+  static_cast<ntp_tiles::MostVisitedSites::Observer*>(handler_.get())
+      ->OnURLsAvailable(false,
+                        {{ntp_tiles::SectionType::PERSONALIZED, {}}});
+  run_loop.Run();
+}
+
 TEST_P(MostVisitedAutoRemovalTest, OnMostVisitedTileHoverMetrics) {
   auto tile = most_visited::mojom::MostVisitedTile::New();
   tile->url = GURL("https://foo.com");
diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
index 860c90cfc2f1b1b362429e261010fbbdd127112d..6a942f3bdc233a525aefd8fad150147a1c1d4df8 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
@@ -1011,6 +1011,7 @@ void NewTabPageUI::RegisterProfilePrefs(PrefRegistrySimple* registry) {
   registry->RegisterBooleanPref(ntp_prefs::kNtpEnterpriseShortcutsVisible,
                                 false);
   registry->RegisterBooleanPref(ntp_prefs::kNtpShortcutsVisible, true);
+  registry->RegisterBooleanPref(ntp_prefs::kNtpAddShortcutVisible, true);
   registry->RegisterIntegerPref(ntp_prefs::kNtpShortcutsStalenessCount, 0);
   registry->RegisterTimePref(ntp_prefs::kNtpLastShortcutsStalenessUpdate,
                              base::Time());
@@ -1036,6 +1037,7 @@ void NewTabPageUI::ResetProfilePrefs(PrefService* prefs) {
   prefs->SetBoolean(ntp_prefs::kNtpCustomLinksVisible, true);
   prefs->SetBoolean(ntp_prefs::kNtpEnterpriseShortcutsVisible, false);
   prefs->SetBoolean(ntp_prefs::kNtpShortcutsVisible, true);
+  prefs->SetBoolean(ntp_prefs::kNtpAddShortcutVisible, true);
   prefs->SetInteger(ntp_prefs::kNtpShortcutsStalenessCount, 0);
   prefs->SetTime(ntp_prefs::kNtpLastShortcutsStalenessUpdate, base::Time());
   prefs->SetBoolean(ntp_prefs::kNtpShortcutsAutoRemovalDisabled, false);
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome.mojom b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome.mojom
index 7a8c2af15fae71d3016e76253dab659b0c3586e5..d3514fd5b4cc87316732bdf6f0f080ffb561f9a3 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome.mojom
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome.mojom
@@ -144,6 +144,9 @@ interface CustomizeChromePageHandler {
   SetMostVisitedSettings(array<ntp_tiles.mojom.TileType> shortcuts_types,
     bool shortcuts_visible, bool personal_shortcuts_visible);
 
+  // Sets whether the add-shortcut tile is shown independently of shortcuts.
+  SetAddShortcutVisible(bool visible);
+
   // Triggers a call to |CustomizeChromePage.SetPageMostVisitedSettings|.
   UpdateMostVisitedSettings();
 
@@ -246,6 +249,7 @@ interface CustomizeChromePage {
   SetMostVisitedSettings(array<ntp_tiles.mojom.TileType> shortcuts_types,
     bool visible,
     bool personal_shortcuts_visible,
+    bool add_shortcut_visible,
     array<ntp_tiles.mojom.TileType> disabled_shortcuts);
   // Sets the tools visibility settings of the NTP tool chips.
   SetToolsSettings(bool visible);
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
index bc5a2cbfc5a268203b858746d5a36bd2c8190926..f01f1fd57d4f0694c52442f214afcabfd60d8889 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
@@ -160,6 +160,11 @@ CustomizeChromePageHandler::CustomizeChromePageHandler(
       base::BindRepeating(
           &CustomizeChromePageHandler::UpdateMostVisitedSettings,
           base::Unretained(this)));
+  pref_change_registrar_.Add(
+      ntp_prefs::kNtpAddShortcutVisible,
+      base::BindRepeating(
+          &CustomizeChromePageHandler::UpdateMostVisitedSettings,
+          base::Unretained(this)));
   pref_change_registrar_.Add(
       ntp_tiles::prefs::kEnterpriseShortcutsPolicyList,
       base::BindRepeating(
@@ -513,6 +518,10 @@ void CustomizeChromePageHandler::SetMostVisitedSettings(
   }
 }
 
+void CustomizeChromePageHandler::SetAddShortcutVisible(bool visible) {
+  profile_->GetPrefs()->SetBoolean(ntp_prefs::kNtpAddShortcutVisible, visible);
+}
+
 void CustomizeChromePageHandler::UpdateMostVisitedSettings() {
   std::vector<ntp_tiles::TileType> disabled_shortcuts;
   // If no enterprise shortcuts are set by policy, hide the enterprise shortcuts
@@ -523,7 +532,9 @@ void CustomizeChromePageHandler::UpdateMostVisitedSettings() {
   auto tile_types = GetTileTypes();
   page_->SetMostVisitedSettings(
       {tile_types.begin(), tile_types.end()}, IsShortcutsVisible(),
-      IsPersonalShortcutsVisible(), std::move(disabled_shortcuts));
+      IsPersonalShortcutsVisible(),
+      profile_->GetPrefs()->GetBoolean(ntp_prefs::kNtpAddShortcutVisible),
+      std::move(disabled_shortcuts));
 }
 
 void CustomizeChromePageHandler::OnBrowserWindowInterfaceChanged() {
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.h b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.h
index 23230fb0bf47dc981caec103ccb7cb6f4ab6ab1b..2ed01c1eada21797e43498bba3dc883272a9cd26 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.h
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.h
@@ -137,6 +137,7 @@ class CustomizeChromePageHandler
   void SetMostVisitedSettings(const std::vector<ntp_tiles::TileType>& types,
                               bool visible,
                               bool personal_shortcuts_visible) override;
+  void SetAddShortcutVisible(bool visible) override;
   void UpdateMostVisitedSettings() override;
   void SetToolChipsVisible(bool visible) override;
   void UpdateToolChipsSettings() override;
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler_unittest.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler_unittest.cc
index 2d0c325fe34cd6981f476a9d242c4331a30c557b..4cf40b818c8a667a0186630b38fd7499aa4d1eb3 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler_unittest.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler_unittest.cc
@@ -175,6 +175,7 @@ class MockPage : public side_panel::mojom::CustomizeChromePage {
   MOCK_METHOD(void,
               SetMostVisitedSettings,
               (const std::vector<ntp_tiles::TileType>&,
+               bool,
                bool,
                bool,
                const std::vector<ntp_tiles::TileType>&));
@@ -461,11 +462,13 @@ TEST_P(CustomizeChromePageHandlerSetMostVisitedTest, SetMostVisitedSettings) {
   std::vector<ntp_tiles::TileType> types;
   bool visible;
   bool personal_shortcuts_visible;
+  bool add_shortcut_visible;
   std::vector<ntp_tiles::TileType> disabled_shortcuts;
   EXPECT_CALL(mock_page_, SetMostVisitedSettings)
       .WillRepeatedly(DoAll(SaveArg<0>(&types), SaveArg<1>(&visible),
                             SaveArg<2>(&personal_shortcuts_visible),
-                            SaveArg<3>(&disabled_shortcuts)));
+                            SaveArg<3>(&add_shortcut_visible),
+                            SaveArg<4>(&disabled_shortcuts)));
 
   // Set initial enterprise shortcuts policy and prefs state.
   SetEnterpriseShortcutsPolicy(test_case.has_enterprise_policy);
@@ -656,6 +659,7 @@ TEST_P(CustomizeChromePageHandlerUpdateMostVisitedTest,
               testing::UnorderedElementsAreArray(test_case.expected_types));
   EXPECT_THAT(disabled_shortcuts, testing::UnorderedElementsAreArray(
                                       test_case.expected_disabled_shortcuts));
+  EXPECT_TRUE(add_shortcut_visible);
 }
 
 const UpdateMostVisitedSettingsTestCase kUpdateMostVisitedSettingsTestCases[] =
@@ -697,11 +701,13 @@ TEST_F(CustomizeChromePageHandlerTest,
   std::vector<ntp_tiles::TileType> types;
   bool visible;
   bool personal_shortcuts_visible;
+  bool add_shortcut_visible;
   std::vector<ntp_tiles::TileType> disabled_shortcuts;
   EXPECT_CALL(mock_page_, SetMostVisitedSettings)
       .WillRepeatedly(DoAll(SaveArg<0>(&types), SaveArg<1>(&visible),
                             SaveArg<2>(&personal_shortcuts_visible),
-                            SaveArg<3>(&disabled_shortcuts)));
+                            SaveArg<3>(&add_shortcut_visible),
+                            SaveArg<4>(&disabled_shortcuts)));
 
   SetEnterpriseShortcutsPolicy(true);
   SetMostVisitedPrefs(
@@ -742,6 +748,19 @@ TEST_F(CustomizeChromePageHandlerTest,
       testing::UnorderedElementsAre(ntp_tiles::TileType::kEnterpriseShortcuts));
 }
 
+TEST_F(CustomizeChromePageHandlerTest, SetAddShortcutVisible) {
+  EXPECT_TRUE(profile().GetPrefs()->GetBoolean(
+      ntp_prefs::kNtpAddShortcutVisible));
+
+  handler().SetAddShortcutVisible(false);
+  EXPECT_FALSE(profile().GetPrefs()->GetBoolean(
+      ntp_prefs::kNtpAddShortcutVisible));
+
+  handler().SetAddShortcutVisible(true);
+  EXPECT_TRUE(profile().GetPrefs()->GetBoolean(
+      ntp_prefs::kNtpAddShortcutVisible));
+}
+
 enum class ThemeUpdateSource {
   kMojo,
   kThemeService,
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_ui.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_ui.cc
index 08d7770d1dd18c471c6338d1480bdcb2699c91ac..3d9adceaf070d5188a7af08bbd1b9fbca7cc7413 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_ui.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_ui.cc
@@ -153,6 +153,7 @@ CustomizeChromeUI::CustomizeChromeUI(content::WebUI* web_ui)
       {"enterpriseShortcutsCurated",
        IDS_NTP_CUSTOMIZE_ENTERPRISE_SHORTCUTS_DESC},
       {"showShortcutsToggle", IDS_NTP_CUSTOMIZE_SHOW_SHORTCUTS_LABEL},
+      {"addShortcutTitle", IDS_NTP_CUSTOM_LINKS_ADD_SHORTCUT_TITLE},
       {"showPersonalShortcutsToggle",
        IDS_NTP_CUSTOMIZE_SHOW_PERSONAL_SHORTCUTS_LABEL},
       {"showPersonalShortcutsToggleDescription",
diff --git a/ui/webui/resources/cr_components/most_visited/most_visited.mojom b/ui/webui/resources/cr_components/most_visited/most_visited.mojom
index 243424f834412e1488c8854c7c41ebb0ff137cb6..c4072a560cc55dc73551af86347bf0451432d60b 100644
--- a/ui/webui/resources/cr_components/most_visited/most_visited.mojom
+++ b/ui/webui/resources/cr_components/most_visited/most_visited.mojom
@@ -34,6 +34,7 @@ struct MostVisitedTheme {
 
 struct MostVisitedInfo {
   bool custom_links_enabled;
+  bool add_shortcut_visible;
   bool enterprise_shortcuts_enabled;
   bool visible;
   array<MostVisitedTile> tiles;
diff --git a/ui/webui/resources/cr_components/most_visited/most_visited.ts b/ui/webui/resources/cr_components/most_visited/most_visited.ts
index b383136a1f346b5398369b30142a6ca0499673a1..beb010f60cd3506156dee8bb5fb85a0b554bdbde 100644
--- a/ui/webui/resources/cr_components/most_visited/most_visited.ts
+++ b/ui/webui/resources/cr_components/most_visited/most_visited.ts
@@ -127,6 +127,8 @@ export class MostVisitedElement extends MostVisitedElementBase {
         reflect: true,
       },
 
+      addShortcutVisible_: {type: Boolean},
+
       enterpriseShortcutsEnabled_: {
         type: Boolean,
         reflect: true,
@@ -198,6 +200,7 @@ export class MostVisitedElement extends MostVisitedElementBase {
   protected accessor columnCount_: number = 3;
   protected accessor rowCount_: number = 1;
   protected accessor customLinksEnabled_: boolean = false;
+  private accessor addShortcutVisible_: boolean = true;
   protected accessor enterpriseShortcutsEnabled_: boolean = false;
   protected accessor dialogTileTitle_: string = '';
   protected accessor dialogTileUrl_: string = '';
@@ -320,6 +323,7 @@ export class MostVisitedElement extends MostVisitedElementBase {
     if (changedPrivateProperties.has('info_') && this.info_ !== null) {
       this.visible_ = this.info_.visible;
       this.customLinksEnabled_ = this.info_.customLinksEnabled;
+      this.addShortcutVisible_ = this.info_.addShortcutVisible;
       this.enterpriseShortcutsEnabled_ = this.info_.enterpriseShortcutsEnabled;
       this.maxTiles_ =
           (this.customLinksEnabled_ ? this.maxShortcutsInExpandedState :
@@ -450,6 +454,9 @@ export class MostVisitedElement extends MostVisitedElementBase {
   }
 
   private computeShowAdd_(): boolean {
+    if (!this.addShortcutVisible_) {
+      return false;
+    }
     if (this.showShowMore_) {
       return false;
     }
