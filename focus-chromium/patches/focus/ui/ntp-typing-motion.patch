diff --git a/chrome/browser/resources/new_tab_page/ntp_searchbox.ts b/chrome/browser/resources/new_tab_page/ntp_searchbox.ts
index 97b1e5e..9730673 100644
--- a/chrome/browser/resources/new_tab_page/ntp_searchbox.ts
+++ b/chrome/browser/resources/new_tab_page/ntp_searchbox.ts
@@ -243,6 +243,61 @@ export class NtpSearchboxElement extends NtpSearchboxElementBase implements
   protected dragAndDropHandler: DragAndDropHandler|null = null;
   protected callbackRouter_: PageCallbackRouter;
 
+  private focusMotionEnabled_: boolean =
+      loadTimeData.getBoolean('focusMotionEnabled');
+  private readonly focusReducedMotionQuery_: MediaQueryList =
+      window.matchMedia('(prefers-reduced-motion: reduce)');
+  private readonly focusForcedColorsQuery_: MediaQueryList =
+      window.matchMedia('(forced-colors: active)');
+  private focusTypingCommittedValue_: string = '';
+  private focusTypingCompositionStartValue_: string|null = null;
+  private focusTypingCompositionFrame_: number|null = null;
+  private focusTypingCompositionGeneration_: number = 0;
+  private readonly focusMotionMediaChangeListener_ = () => {
+    if (this.focusReducedMotionQuery_.matches ||
+        this.focusForcedColorsQuery_.matches) {
+      this.shadowRoot
+          ?.querySelector<SearchboxInputElement>('#input')
+          ?.cancelFocusTypingReveal();
+    }
+  };
+  private readonly focusTypingCompositionStartListener_ = () => {
+    if (this.focusTypingCompositionFrame_ !== null) {
+      cancelAnimationFrame(this.focusTypingCompositionFrame_);
+      this.focusTypingCompositionFrame_ = null;
+    }
+    this.focusTypingCompositionGeneration_++;
+    const value = this.$.input.inputElement.value;
+    this.$.input.cancelFocusTypingReveal();
+    this.focusTypingCommittedValue_ = value;
+    this.focusTypingCompositionStartValue_ = value;
+  };
+  private readonly focusTypingCompositionEndListener_ = () => {
+    const baseline = this.focusTypingCompositionStartValue_;
+    if (baseline === null) {
+      return;
+    }
+
+    const generation = this.focusTypingCompositionGeneration_;
+    this.focusTypingCompositionFrame_ = requestAnimationFrame(() => {
+      this.focusTypingCompositionFrame_ = null;
+      if (generation !== this.focusTypingCompositionGeneration_ ||
+          this.focusTypingCompositionStartValue_ === null) {
+        return;
+      }
+
+      // Wait until the frame after compositionend so the final input event has
+      // settled. A confirmed composition differs from its pre-composition
+      // value; a cancelled composition returns to it.
+      const value = this.$.input.inputElement.value;
+      this.focusTypingCompositionStartValue_ = null;
+      this.focusTypingCommittedValue_ = value;
+      if (value !== baseline) {
+        this.playFocusTypingAnimation_(baseline, value);
+      }
+    });
+  };
+
   private placeholderCycler_: PlaceholderTextCycler|null = null;
   private dragAndDropEnabled_: boolean =
       loadTimeData.getBoolean('composeboxContextDragAndDropEnabled');
@@ -262,6 +317,19 @@ export class NtpSearchboxElement extends NtpSearchboxElementBase implements
 
   override async connectedCallback() {
     super.connectedCallback();
+    this.addWebUiListener(
+        'focus-motion-enabled-changed', (enabled: boolean) => {
+          this.focusMotionEnabled_ = enabled;
+          if (!enabled) {
+            this.shadowRoot
+                ?.querySelector<SearchboxInputElement>('#input')
+                ?.cancelFocusTypingReveal();
+          }
+        });
+    this.focusReducedMotionQuery_.addEventListener(
+        'change', this.focusMotionMediaChangeListener_);
+    this.focusForcedColorsQuery_.addEventListener(
+        'change', this.focusMotionMediaChangeListener_);
     this.autocompleteResultChangedListenerId_ =
         this.callbackRouter_.autocompleteResultChanged.addListener(
             this.onAutocompleteResultChanged.bind(this));
@@ -305,6 +373,19 @@ export class NtpSearchboxElement extends NtpSearchboxElementBase implements
       this.inputStateListenerId_ = null;
     }
 
+    this.focusReducedMotionQuery_.removeEventListener(
+        'change', this.focusMotionMediaChangeListener_);
+    this.focusForcedColorsQuery_.removeEventListener(
+        'change', this.focusMotionMediaChangeListener_);
+    this.shadowRoot
+        ?.querySelector<SearchboxInputElement>('#input')
+        ?.cancelFocusTypingReveal();
+    if (this.focusTypingCompositionFrame_ !== null) {
+      cancelAnimationFrame(this.focusTypingCompositionFrame_);
+      this.focusTypingCompositionFrame_ = null;
+    }
+    this.focusTypingCompositionGeneration_++;
+    this.focusTypingCompositionStartValue_ = null;
     this.placeholderCycler_?.stop();
     if (this.onTabStripChangedListenerId_ !== null) {
       this.callbackRouter_.removeListener(this.onTabStripChangedListenerId_);
@@ -335,6 +416,11 @@ export class NtpSearchboxElement extends NtpSearchboxElementBase implements
       performance.measure('realbox-creation', 'realbox-creation-start');
     }
     this.initialInputScrollHeight = this.$.input.scrollHeight;
+    this.focusTypingCommittedValue_ = this.$.input.inputElement.value;
+    this.$.input.inputElement.addEventListener(
+        'compositionstart', this.focusTypingCompositionStartListener_);
+    this.$.input.inputElement.addEventListener(
+        'compositionend', this.focusTypingCompositionEndListener_);
 
     if (this.cyclingPlaceholders) {
       waitForLazyRender().then(async () => {
@@ -740,6 +826,25 @@ export class NtpSearchboxElement extends NtpSearchboxElementBase implements
   protected onSearchboxInputTextUpdated_(
       e: CustomEvent<{value: string, isComposing: boolean}>) {
     this.onSearchboxInputTextUpdated(e, /*is_composing=*/ false);
+    if (e.detail.isComposing ||
+        this.focusTypingCompositionStartValue_ !== null ||
+        e.detail.value === this.focusTypingCommittedValue_) {
+      return;
+    }
+
+    const previousValue = this.focusTypingCommittedValue_;
+    this.focusTypingCommittedValue_ = e.detail.value;
+    this.playFocusTypingAnimation_(previousValue, e.detail.value);
+  }
+
+  private playFocusTypingAnimation_(
+      previousValue: string, currentValue: string) {
+    if (!this.focusMotionEnabled_ || this.focusReducedMotionQuery_.matches ||
+        this.focusForcedColorsQuery_.matches) {
+      this.$.input.cancelFocusTypingReveal();
+      return;
+    }
+    this.$.input.revealFocusTypingInsertion(previousValue, currentValue);
   }
 
   protected onLensSearchClick_() {
diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
index 38c1ee4..c013d86 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
@@ -91,6 +91,7 @@
 #include "components/content_settings/core/common/content_settings_pattern.h"
 #include "components/contextual_search/contextual_search_metrics_recorder.h"
 #include "components/contextual_search/contextual_search_service.h"
+#include "components/focus_services/pref_names.h"
 #include "components/favicon_base/favicon_url_parser.h"
 #include "components/google/core/common/google_util.h"
 #include "components/grit/components_scaled_resources.h"
@@ -205,6 +206,9 @@ content::WebUIDataSource* CreateAndAddNewTabPageUiHtmlSource(
   content::WebUIDataSource* source = content::WebUIDataSource::CreateAndAdd(
       profile, chrome::kChromeUINewTabPageHost);
 
+  source->AddBoolean(
+      "focusMotionEnabled",
+      profile->GetPrefs()->GetBoolean(prefs::kFocusMotionEnabled));
   ui::Accelerator undo_accelerator(ui::VKEY_Z, ui::EF_PLATFORM_ACCELERATOR);
   source->AddString("undoDescription", l10n_util::GetStringFUTF16(
                                            IDS_UNDO_DESCRIPTION,
@@ -955,6 +959,10 @@ NewTabPageUI::NewTabPageUI(content::WebUI* web_ui)
       ntp_prefs::kNtpShortcutsVisible,
       base::BindRepeating(&NewTabPageUI::OnTilesVisibilityPrefChanged,
                           weak_ptr_factory_.GetWeakPtr()));
+  pref_change_registrar_.Add(
+      prefs::kFocusMotionEnabled,
+      base::BindRepeating(&NewTabPageUI::OnFocusMotionEnabledChanged,
+                          weak_ptr_factory_.GetWeakPtr()));
 // TODO(b/502297163): Implement for Android.
 #if !BUILDFLAG(IS_ANDROID)
   pref_change_registrar_.Add(
@@ -1504,6 +1512,12 @@ void NewTabPageUI::OnTilesVisibilityPrefChanged() {
   }
 }
 
+void NewTabPageUI::OnFocusMotionEnabledChanged() {
+  web_ui()->CallJavascriptFunctionUnsafe(
+      "cr.webUIListenerCallback", "focus-motion-enabled-changed",
+      profile_->GetPrefs()->GetBoolean(prefs::kFocusMotionEnabled));
+}
+
 void NewTabPageUI::OnEnterpriseShortcutsPolicyChanged() {
   MaybeEnableEnterpriseShortcutsVisibility();
   OnTileTypesChanged();
diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
index e7bb7b4..087a130 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
@@ -326,6 +326,8 @@ class NewTabPageUI
   void OnTileTypesChanged();
   // Callback for when the value of the pref for showing the NTP tiles changes.
   void OnTilesVisibilityPrefChanged();
+  // Called when Focus Browser motion effects are enabled or disabled.
+  void OnFocusMotionEnabledChanged();
   // Called when the enterprise shortcuts policy may have changed.
   void OnEnterpriseShortcutsPolicyChanged();
   // Called when the NTP (re)loads. Sets mutable load time data.
diff --git a/ui/webui/resources/cr_components/searchbox/searchbox_input.css b/ui/webui/resources/cr_components/searchbox/searchbox_input.css
index 419343e..960273a 100644
--- a/ui/webui/resources/cr_components/searchbox/searchbox_input.css
+++ b/ui/webui/resources/cr_components/searchbox/searchbox_input.css
@@ -103,9 +103,46 @@ cr-searchbox-icon {
   z-index: 100;
 }
 
-.truncate {
+#focusTypingMirror {
+  background: transparent;
+  color: var(--color-searchbox-foreground);
   overflow: hidden;
-  text-overflow: ellipsis;
+  pointer-events: none;
+  position: absolute;
+  z-index: 103;
 }
 
+#focusTypingMirror[hidden] {
+  display: none;
+}
 
+#focusTypingMirrorText {
+  display: inline-block;
+  min-width: max-content;
+  transform-origin: top left;
+}
+
+#focusTypingMirrorText > span {
+  display: inline-block;
+  white-space: pre;
+}
+
+:host([focus-typing-reveal-active]) #input {
+  -webkit-text-fill-color: transparent;
+  caret-color: var(--color-searchbox-foreground);
+}
+
+@media (prefers-reduced-motion: reduce), (forced-colors: active) {
+  #focusTypingMirror {
+    display: none !important;
+  }
+
+  :host([focus-typing-reveal-active]) #input {
+    -webkit-text-fill-color: currentColor;
+  }
+}
+
+.truncate {
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
diff --git a/ui/webui/resources/cr_components/searchbox/searchbox_input.html.ts b/ui/webui/resources/cr_components/searchbox/searchbox_input.html.ts
index f78c9a2..109bdb2 100644
--- a/ui/webui/resources/cr_components/searchbox/searchbox_input.html.ts
+++ b/ui/webui/resources/cr_components/searchbox/searchbox_input.html.ts
@@ -24,11 +24,15 @@ export function getHtml(this: SearchboxInputElement) {
           placeholder="${this.computePlaceholderText_()}"
           @copy="${this.onInputCopy_}"
           @cut="${this.onInputCut_}"
+          @blur="${this.onFocusTypingInputBlur_}"
+          @compositionstart="${this.onFocusTypingCompositionstart_}"
           @input="${this.onInputInput_}"
           @keydown="${this.onInputKeydown_}"
           @keyup="${this.onInputKeyup_}"
           @mousedown="${this.onInputMousedown_}"
-          @paste="${this.onInputPaste_}"></textarea>
+          @paste="${this.onInputPaste_}"
+          @scroll="${this.onFocusTypingInputScroll_}"
+          @select="${this.onFocusTypingInputSelect_}"></textarea>
     ` : html`
       <input id="input" class="truncate" type="search" autocomplete="off"
           part="searchbox-input"
@@ -38,12 +42,19 @@ export function getHtml(this: SearchboxInputElement) {
           placeholder="${this.computePlaceholderText_()}"
           @copy="${this.onInputCopy_}"
           @cut="${this.onInputCut_}"
+          @blur="${this.onFocusTypingInputBlur_}"
+          @compositionstart="${this.onFocusTypingCompositionstart_}"
           @input="${this.onInputInput_}"
           @keydown="${this.onInputKeydown_}"
           @keyup="${this.onInputKeyup_}"
           @mousedown="${this.onInputMousedown_}"
-          @paste="${this.onInputPaste_}">
+          @paste="${this.onInputPaste_}"
+          @scroll="${this.onFocusTypingInputScroll_}"
+          @select="${this.onFocusTypingInputSelect_}">
     `}
+    <div id="focusTypingMirror" aria-hidden="true" hidden>
+      <span id="focusTypingMirrorText"></span>
+    </div>
     <slot name="action-buttons"></slot>
     <slot name="compose-button"></slot>
 </div>
diff --git a/ui/webui/resources/cr_components/searchbox/searchbox_input.ts b/ui/webui/resources/cr_components/searchbox/searchbox_input.ts
index 984befb..ac0eda2 100644
--- a/ui/webui/resources/cr_components/searchbox/searchbox_input.ts
+++ b/ui/webui/resources/cr_components/searchbox/searchbox_input.ts
@@ -42,6 +42,8 @@ const SearchboxInputElementBase = I18nMixinLit(CrLitElement);
 
 export interface SearchboxInputElement {
   $: {
+    focusTypingMirror: HTMLDivElement,
+    focusTypingMirrorText: HTMLSpanElement,
     input: HTMLInputElement|HTMLTextAreaElement,
     icon: SearchboxIconElement,
   };
@@ -89,6 +91,10 @@ export class SearchboxInputElement extends SearchboxInputElementBase {
   private lastInput_: Input = {text: '', inline: ''};
   private isDeletingInput_: boolean = false;
   private pastedInInput_: boolean = false;
+  private focusTypingAnimations_: Set<Animation> = new Set();
+  private focusTypingMirrorGraphemes_: string[] = [];
+  private readonly focusTypingSegmenter_: Intl.Segmenter =
+      new Intl.Segmenter(undefined, {granularity: 'grapheme'});
 
   constructor() {
     super();
@@ -161,6 +167,195 @@ export class SearchboxInputElement extends SearchboxInputElementBase {
         this.$.input.scrollHeight > MULTILINE_INPUT_HEIGHT_THRESHOLD;
   }
 
+  /**
+   * Reveals only the graphemes inserted by the latest committed edit. The
+   * real input remains the sole editable and accessible control; this mirror
+   * is aria-hidden and exists only for the short visual transition.
+   */
+  revealFocusTypingInsertion(previousValue: string, currentValue: string) {
+    const input = this.$.input;
+    if (input.value !== currentValue ||
+        input.selectionStart !== input.selectionEnd) {
+      this.cancelFocusTypingReveal();
+      return;
+    }
+
+    const previousGraphemes = this.segmentFocusTypingText_(previousValue);
+    const currentGraphemes = this.segmentFocusTypingText_(currentValue);
+    if (currentGraphemes.length > 512) {
+      this.cancelFocusTypingReveal();
+      return;
+    }
+
+    let prefixLength = 0;
+    while (prefixLength < previousGraphemes.length &&
+           prefixLength < currentGraphemes.length &&
+           previousGraphemes[prefixLength] ===
+               currentGraphemes[prefixLength]) {
+      prefixLength++;
+    }
+
+    let suffixLength = 0;
+    while (suffixLength < previousGraphemes.length - prefixLength &&
+           suffixLength < currentGraphemes.length - prefixLength &&
+           previousGraphemes[previousGraphemes.length - suffixLength - 1] ===
+               currentGraphemes[currentGraphemes.length - suffixLength - 1]) {
+      suffixLength++;
+    }
+
+    const insertedGraphemes = currentGraphemes.slice(
+        prefixLength, currentGraphemes.length - suffixLength);
+    if (insertedGraphemes.length === 0) {
+      this.cancelFocusTypingReveal();
+      return;
+    }
+
+    const mirrorText = this.$.focusTypingMirrorText;
+    const canReuseMirror =
+        this.hasAttribute('focus-typing-reveal-active') &&
+        this.focusTypingMirrorGraphemes_.length ===
+            previousGraphemes.length &&
+        this.focusTypingMirrorGraphemes_.every(
+            (grapheme, index) => grapheme === previousGraphemes[index]);
+
+    let insertedElements: HTMLSpanElement[] = [];
+    if (canReuseMirror) {
+      const oldElements =
+          Array.from(mirrorText.children) as HTMLSpanElement[];
+      const removedElements = oldElements.slice(
+          prefixLength, previousGraphemes.length - suffixLength);
+      for (const animation of Array.from(this.focusTypingAnimations_)) {
+        const target = (animation.effect as KeyframeEffect|null)?.target;
+        if (target instanceof HTMLSpanElement &&
+            removedElements.includes(target)) {
+          this.focusTypingAnimations_.delete(animation);
+          animation.cancel();
+        }
+      }
+      for (const element of removedElements) {
+        element.remove();
+      }
+
+      const anchor = mirrorText.children[prefixLength] ?? null;
+      const fragment = document.createDocumentFragment();
+      insertedElements = insertedGraphemes.map(grapheme => {
+        const element = document.createElement('span');
+        element.textContent = grapheme;
+        fragment.appendChild(element);
+        return element;
+      });
+      mirrorText.insertBefore(fragment, anchor);
+    } else {
+      this.cancelFocusTypingReveal();
+      const fragment = document.createDocumentFragment();
+      const elements = currentGraphemes.map(grapheme => {
+        const element = document.createElement('span');
+        element.textContent = grapheme;
+        fragment.appendChild(element);
+        return element;
+      });
+      mirrorText.replaceChildren(fragment);
+      insertedElements = elements.slice(
+          prefixLength, currentGraphemes.length - suffixLength);
+    }
+
+    this.focusTypingMirrorGraphemes_ = currentGraphemes;
+    this.syncFocusTypingMirrorStyle_();
+    this.$.focusTypingMirror.hidden = false;
+    this.toggleAttribute('focus-typing-reveal-active', true);
+
+    // Keep very large pastes inexpensive. Normal typing and IME commits are
+    // still animated grapheme-by-grapheme, including emoji and combining text.
+    const animatedElements = insertedElements.slice(0, 24);
+    for (const [index, element] of animatedElements.entries()) {
+      const animation = element.animate(
+          [
+            {opacity: 0.12, transform: 'translateY(3px)'},
+            {opacity: 1, transform: 'translateY(0)'},
+          ],
+          {
+            delay: Math.min(index, 10) * 12,
+            duration: 180,
+            easing: 'cubic-bezier(0.22, 1, 0.36, 1)',
+            fill: 'both',
+          });
+      animation.id = 'focus-typing-grapheme-reveal';
+      this.focusTypingAnimations_.add(animation);
+      animation.finished.then(
+          () => this.onFocusTypingAnimationFinished_(animation),
+          () => this.onFocusTypingAnimationFinished_(animation));
+    }
+  }
+
+  cancelFocusTypingReveal() {
+    for (const animation of this.focusTypingAnimations_) {
+      animation.cancel();
+    }
+    this.focusTypingAnimations_.clear();
+    this.focusTypingMirrorGraphemes_ = [];
+    this.$.focusTypingMirrorText.replaceChildren();
+    this.$.focusTypingMirrorText.style.transform = '';
+    this.$.focusTypingMirror.hidden = true;
+    this.toggleAttribute('focus-typing-reveal-active', false);
+  }
+
+  private segmentFocusTypingText_(value: string): string[] {
+    return Array.from(
+        this.focusTypingSegmenter_.segment(value), item => item.segment);
+  }
+
+  private syncFocusTypingMirrorStyle_() {
+    const input = this.$.input;
+    const mirror = this.$.focusTypingMirror;
+    const style = getComputedStyle(input);
+    const copiedProperties = [
+      'color',
+      'direction',
+      'font-family',
+      'font-feature-settings',
+      'font-kerning',
+      'font-size',
+      'font-stretch',
+      'font-style',
+      'font-variant',
+      'font-weight',
+      'letter-spacing',
+      'line-height',
+      'padding-bottom',
+      'padding-left',
+      'padding-right',
+      'padding-top',
+      'text-align',
+      'text-indent',
+      'text-transform',
+      'white-space',
+      'word-spacing',
+      'writing-mode',
+    ];
+    for (const property of copiedProperties) {
+      mirror.style.setProperty(property, style.getPropertyValue(property));
+    }
+    mirror.style.boxSizing = 'border-box';
+    mirror.style.height = `${input.offsetHeight}px`;
+    mirror.style.left = `${input.offsetLeft}px`;
+    mirror.style.top = `${input.offsetTop}px`;
+    mirror.style.width = `${input.offsetWidth}px`;
+    this.syncFocusTypingMirrorScroll_();
+  }
+
+  private syncFocusTypingMirrorScroll_() {
+    this.$.focusTypingMirrorText.style.transform =
+        `translate(${-this.$.input.scrollLeft}px, ` +
+        `${-this.$.input.scrollTop}px)`;
+  }
+
+  private onFocusTypingAnimationFinished_(animation: Animation) {
+    this.focusTypingAnimations_.delete(animation);
+    if (this.focusTypingAnimations_.size === 0) {
+      this.cancelFocusTypingReveal();
+    }
+  }
+
   preventInlineAutocomplete(input: string) {
     const caretNotAtEnd =
         this.$.input ? this.$.input.selectionStart !== input.length : false;
@@ -311,6 +506,24 @@ export class SearchboxInputElement extends SearchboxInputElementBase {
     this.pastedInInput_ = true;
   }
 
+  protected onFocusTypingInputBlur_() {
+    this.cancelFocusTypingReveal();
+  }
+
+  protected onFocusTypingCompositionstart_() {
+    this.cancelFocusTypingReveal();
+  }
+
+  protected onFocusTypingInputScroll_() {
+    this.syncFocusTypingMirrorScroll_();
+  }
+
+  protected onFocusTypingInputSelect_() {
+    if (this.$.input.selectionStart !== this.$.input.selectionEnd) {
+      this.cancelFocusTypingReveal();
+    }
+  }
+
   /**
    * Updates the input state (text and inline autocompletion) with |update|.
    */
@@ -326,8 +539,10 @@ export class SearchboxInputElement extends SearchboxInputElementBase {
     const oldSelectionStart = this.$.input?.selectionStart || null;
     const oldSelectionEnd = this.$.input?.selectionEnd || null;
 
+    let inputValueWasSet = false;
     if (this.$.input && newInputValue !== this.$.input.value) {
       this.$.input.value = newInputValue;
+      inputValueWasSet = true;
       needsSelectionUpdate = true;  // Setting .value blows away selection.
     }
 
@@ -344,6 +559,10 @@ export class SearchboxInputElement extends SearchboxInputElementBase {
     this.isDeletingInput_ = lastInputValue.length > newInputValue.length &&
         lastInputValue.startsWith(newInputValue);
     this.lastInput_ = newInput;
+    if (inputValueWasSet &&
+        this.hasAttribute('focus-typing-reveal-active')) {
+      this.cancelFocusTypingReveal();
+    }
   }
 
   protected computePlaceholderText_(): string {

