--- a/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
+++ b/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
@@ -24,8 +24,10 @@
 #include "base/task/thread_pool.h"
 #include "base/threading/scoped_blocking_call.h"
 #include "build/build_config.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/spellchecker/spellcheck_service.h"
 #include "chrome/common/chrome_paths.h"
+#include "components/focus_services/focus_services_helpers.h"
 #include "components/spellcheck/browser/spellcheck_platform.h"
 #include "components/spellcheck/common/spellcheck_common.h"
 #include "components/spellcheck/common/spellcheck_features.h"
@@ -277,15 +279,26 @@ GURL SpellcheckHunspellDictionary::GetDi
   std::string bdict_file = dictionary_file_.path.BaseName().MaybeAsASCII();
   DCHECK(!bdict_file.empty());
 
-  static const char kDownloadServerUrl[] =
-      "https://redirector.gvt1.com/edgedl/chrome/dict/";
+  if (browser_context_) {
+    const Profile* profile = Profile::FromBrowserContext(browser_context_);
+    if (!profile) {
+      goto fail;
+    }
+
+    const PrefService* prefs = profile->GetPrefs();
+    if (!prefs) {
+      goto fail;
+    }
+
+    return focus::GetSpellcheckURL(*prefs)
+            .Resolve(bdict_file);
+  }
 
-  return GURL(std::string(kDownloadServerUrl) +
-              base::ToLowerASCII(bdict_file));
+fail:
+  return GURL("https://failed-getting-spellcheck.qjz9zk/");
 }
 
 void SpellcheckHunspellDictionary::DownloadDictionary(GURL url) {
-  return;
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   DCHECK(browser_context_);
 
--- a/components/focus_services/focus_services_helpers.cc
+++ b/components/focus_services/focus_services_helpers.cc
@@ -63,6 +63,11 @@ bool ShouldAccessExtensionService(const
             prefs.GetBoolean(prefs::kFocusExtProxyEnabled);
 }
 
+bool ShouldAccessDictionaryService(const PrefService& prefs) {
+    return ShouldAccessServices(prefs) &&
+            prefs.GetBoolean(prefs::kFocusSpellcheckEnabled);
+}
+
 bool ShouldFetchBangs(const PrefService& prefs) {
     return ShouldAccessServices(prefs) &&
             prefs.GetBoolean(prefs::kFocusBangsEnabled);
@@ -85,6 +90,14 @@ GURL GetWebstoreSnippetURL(const PrefSer
         base::StringPrintf("/ext/cws_snippet?id=%s", id));
 }
 
+GURL GetSpellcheckURL(const PrefService& prefs) {
+    if (!ShouldAccessDictionaryService(prefs)) {
+        return GetDummyURL();
+    }
+
+    return GetServicesBaseURL(prefs).Resolve("/dict/");
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kFocusServicesEnabled, observer);
--- a/components/focus_services/focus_services_helpers.h
+++ b/components/focus_services/focus_services_helpers.h
@@ -26,6 +26,7 @@ COMPONENT_EXPORT(FOCUS) GURL GetService
 COMPONENT_EXPORT(FOCUS) GURL GetDummyURL();
 COMPONENT_EXPORT(FOCUS) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(FOCUS) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
+COMPONENT_EXPORT(FOCUS) GURL GetSpellcheckURL(const PrefService& prefs);
 COMPONENT_EXPORT(FOCUS) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(FOCUS) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/focus_services/pref_names.h
+++ b/components/focus_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kFocusExtProxyEna
 inline constexpr char kFocusBangsEnabled[] =
     "focus.services.bangs";
 
+inline constexpr char kFocusSpellcheckEnabled[] =
+    "focus.services.spellcheck_files";
+
 }  // namespace prefs
 
 #endif  // COMPONENTS_FOCUS_SERVICES_PREF_NAMES_H_
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -5919,6 +5919,8 @@ static_library("test_support") {
     deps += [
       "//chrome/browser/spellchecker",
       "//components/spellcheck/common",
+      "//components/focus_services",
+      "//chrome/browser/profiles:profile",
     ]
   }
   if (enable_bound_session_credentials) {
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -419,6 +419,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kFocusBangsEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kFocusSpellcheckEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kFocusServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kFocusServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -229,6 +229,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterBooleanPref(prefs::kFocusServicesEnabled, true);
     registry->RegisterBooleanPref(prefs::kFocusBangsEnabled, true);
     registry->RegisterBooleanPref(prefs::kFocusExtProxyEnabled, true);
+    registry->RegisterBooleanPref(prefs::kFocusSpellcheckEnabled, true);
     registry->RegisterStringPref(prefs::kFocusServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kFocusDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kFocusServicesConsented, false);
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -92,6 +92,13 @@
                 label="$i18n{focusBangsToggle}"
                 sub-label="$i18n{focusBangsToggleDescription}">
             </settings-toggle-button>
+            <if expr="use_renderer_spellchecker">
+                <settings-toggle-button id="spellcheckToggleButton"
+                    pref="{{prefs.focus.services.spellcheck_files}}"
+                    label="$i18n{focusSpellcheckToggle}"
+                    sub-label="$i18n{focusSpellcheckToggleDescription}">
+                </settings-toggle-button>
+            </if>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1285,6 +1285,9 @@
         <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
           Please check with your network administrator to make sure that the firewall is not blocking downloads from Google servers.
         </message>
+        <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_FOCUS" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
+          Please check that Focus Browser services are enabled (including spell check downloads) and make sure that the firewall is not blocking downloads from Focus Browser servers.
+        </message>
       </if>
     </if>
     <if expr="is_chromeos">
@@ -1736,6 +1739,12 @@
     <message name="IDS_SETTINGS_FOCUS_SERVICES_BANGS_TOGGLE_DESCRIPTION" desc="Description of the toggle for enabling/disabling of downloading bangs">
       Focus Browser will fetch a list of bangs that help you browse the Internet faster, such as !w or !gh. When disabled, bangs will not work.
     </message>
+    <message name="IDS_SETTINGS_FOCUS_SERVICES_SPELLCHECK_TOGGLE" desc="Toggle for enabling/disabling of downloading spellcheck files">
+      Allow downloading dictionary files for spell checking
+    </message>
+    <message name="IDS_SETTINGS_FOCUS_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION" desc="Description of the for enabling/disabling of downloading spellcheck files">
+      Focus Browser will fetch dictionary files used for spell checking when requested. When disabled, spell checking will not work.
+    </message>
     <message name="IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE" desc="Text input for overriding the Focus Browser services server">
       Use your own instance of Focus Browser services
     </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -1379,7 +1379,7 @@ void AddLanguagesStrings(content::WebUID
       {"languagesDictionaryDownloadError",
        IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED},
       {"languagesDictionaryDownloadErrorHelp",
-       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP},
+       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_FOCUS},
 #endif
 #endif  // !BUILDFLAG(IS_CHROMEOS)
 #if BUILDFLAG(IS_CHROMEOS)
@@ -2358,6 +2358,9 @@ void AddPrivacyStrings(content::WebUIDat
       {"focusBangsToggle", IDS_SETTINGS_FOCUS_SERVICES_BANGS_TOGGLE},
       {"focusBangsToggleDescription",
         IDS_SETTINGS_FOCUS_SERVICES_BANGS_TOGGLE_DESCRIPTION},
+      {"focusSpellcheckToggle", IDS_SETTINGS_FOCUS_SERVICES_SPELLCHECK_TOGGLE},
+      {"focusSpellcheckToggleDescription",
+        IDS_SETTINGS_FOCUS_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION},
       {"focusOriginOverride", IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE},
       {"focusOriginOverrideDescription",
         IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE_DESCRIPTION},
--- a/chrome/common/features.gni
+++ b/chrome/common/features.gni
@@ -9,6 +9,7 @@ import("//build/config/features.gni")
 import("//components/compose/features.gni")
 import("//components/safe_browsing/buildflags.gni")
 import("//components/signin/features.gni")
+import("//components/spellcheck/spellcheck_build_features.gni")
 import("//components/supervised_user/buildflags.gni")
 import("//content/public/common/features.gni")
 import("//crypto/features.gni")
@@ -111,4 +112,5 @@ chrome_grit_defines = [
   "chrome_root_store_cert_management_ui=$chrome_root_store_cert_management_ui",
   "use_nss_certs=$use_nss_certs",
   "webnn_enable_graph_dump=$webnn_enable_graph_dump",
+  "use_renderer_spellchecker=$use_renderer_spellchecker",
 ]
