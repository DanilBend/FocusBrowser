--- a/components/focus_services/pref_names.h
+++ b/components/focus_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kFocusExtProxyEna
 inline constexpr char kFocusBangsEnabled[] =
     "focus.services.bangs";
 
+inline constexpr char kFocusUpdateFetchingEnabled[] =
+    "focus.services.browser_updates";
+
 inline constexpr char kFocusSpellcheckEnabled[] =
     "focus.services.spellcheck_files";
 
--- a/components/focus_services/focus_services_helpers.h
+++ b/components/focus_services/focus_services_helpers.h
@@ -22,6 +22,7 @@ bool ShouldAccessServices(const PrefServ
 
 COMPONENT_EXPORT(FOCUS) bool ShouldFetchBangs(const PrefService& prefs);
 COMPONENT_EXPORT(FOCUS) bool ShouldAccessExtensionService(const PrefService& prefs);
+COMPONENT_EXPORT(FOCUS) bool ShouldAccessUpdateService(const PrefService& prefs);
 COMPONENT_EXPORT(FOCUS) GURL GetServicesBaseURL(const PrefService& prefs);
 COMPONENT_EXPORT(FOCUS) GURL GetDummyURL();
 COMPONENT_EXPORT(FOCUS) GURL GetExtensionUpdateURL(const PrefService& prefs);
--- a/components/focus_services/focus_services_helpers.cc
+++ b/components/focus_services/focus_services_helpers.cc
@@ -73,6 +73,12 @@ bool ShouldFetchBangs(const PrefService&
             prefs.GetBoolean(prefs::kFocusBangsEnabled);
 }
 
+bool ShouldAccessUpdateService(const PrefService& prefs) {
+  // Browser updates are a public, signed service and are deliberately
+  // independent from Focus's optional private-services consent.
+  return prefs.GetBoolean(prefs::kFocusUpdateFetchingEnabled);
+}
+
 GURL GetExtensionUpdateURL(const PrefService& prefs) {
     if (!ShouldAccessExtensionService(prefs)) {
         return GetDummyURL();
--- a/chrome/browser/resources/settings/privacy_page/privacy_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/privacy_page.ts
@@ -15,6 +15,7 @@ import 'chrome://resources/cr_elements/c
 import 'chrome://resources/cr_elements/cr_toast/cr_toast.js';
 import '../icons.html.js';
 import '../privacy_icons.html.js';
+import '../controls/settings_toggle_button.js';
 import '../settings_page/settings_section.js';
 import '../settings_shared.css.js';
 import './privacy_guide/privacy_guide_dialog.js';
@@ -64,6 +65,11 @@ export class SettingsPrivacyPageElement
       showClearBrowsingDataDialog_: Boolean,
       showPrivacyGuideDialog_: Boolean,
 
+      focusUpdaterAvailable_: {
+        type: Boolean,
+        value: () => loadTimeData.getBoolean('focusUpdaterAvailable'),
+      },
+
       isPrivacySandboxRestricted_: {
         type: Boolean,
         value: () => loadTimeData.getBoolean('isPrivacySandboxRestricted'),
@@ -96,6 +102,7 @@ export class SettingsPrivacyPageElement
 
   declare private showClearBrowsingDataDialog_: boolean;
   declare private showPrivacyGuideDialog_: boolean;
+  declare private focusUpdaterAvailable_: boolean;
   declare private isPrivacySandboxRestricted_: boolean;
   declare private isPrivacySandboxRestrictedNoticeEnabled_: boolean;
   declare private isAdPrivacyAvailable_: boolean;
--- a/chrome/browser/resources/settings/privacy_page/privacy_page.html
+++ b/chrome/browser/resources/settings/privacy_page/privacy_page.html
@@ -49,8 +49,15 @@
     <cr-link-row id="focusServicesLinkRow" start-icon="cr:chrome-product"
         class="hr" label="$i18n{focusServicesTitle}"
         sub-label="$i18n{focusServicesDescription}"
         on-click="onFocusServicesPageClick_"
         role-description="$i18n{subpageArrowRoleDescription}"></cr-link-row>
+    <template is="dom-if" if="[[focusUpdaterAvailable_]]">
+      <settings-toggle-button id="focusBrowserUpdatesToggle" class="hr"
+          pref="{{prefs.focus.services.browser_updates}}"
+          label="$i18n{focusBrowserUpdatesLabel}"
+          sub-label="$i18n{focusBrowserUpdatesDescription}">
+      </settings-toggle-button>
+    </template>
     <cr-toast id="deleteBrowsingDataToast" duration="5000">
       <div>[[dbdDeletionConfirmationToastLabel_]]</div>
     </cr-toast>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -17,8 +17,9 @@
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
 #include "build/buildflag.h"
 #include "chrome/browser/autofill/personal_data_manager_factory.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/browser/browser_process_platform_part.h"
 #include "chrome/browser/content_settings/host_content_settings_map_factory.h"
+#include "chrome/browser/buildflags.h"
 #include "chrome/browser/contextual_cueing/features.h"
@@ -2367,10 +2368,13 @@ void AddPrivacyStrings(content::WebUIDat
       {"focusOriginOverrideDescriptionWarning",
         IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE_DESCRIPTION_WARNING},
       {"focusOriginOverrideLabel",
         IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE_ARIA_LABEL},
       {"focusOriginOverrideInvalidOrigin",
         IDS_SETTINGS_FOCUS_SERVICES_OVERRIDE_INVALID_ORIGIN},
+      {"focusBrowserUpdatesLabel", IDS_SETTINGS_FOCUS_BROWSER_UPDATES},
+      {"focusBrowserUpdatesDescription",
+       IDS_SETTINGS_FOCUS_BROWSER_UPDATES_DESCRIPTION},
       {"advancedProtectionProgramTitle",
        IDS_SETTINGS_ADVANCED_PROTECTION_PROGRAM},
       {"advancedProtectionProgramDesc",
        IDS_SETTINGS_ADVANCED_PROTECTION_PROGRAM_DESC},
@@ -2636,6 +2640,14 @@ void AddPrivacyStrings(content::WebUIDat
        IDS_SETTINGS_SECURITY_JAVASCRIPT_GUARDRAILS_MANAGE_SITE_EXCEPTIONS_DESCRIPTION}};
   html_source->AddLocalizedStrings(kLocalizedStrings);
 
+  html_source->AddBoolean("focusUpdaterAvailable",
+#if BUILDFLAG(IS_WIN)
+                          BUILDFLAG(ENABLE_UPDATE_NOTIFICATIONS)
+#else
+                          false
+#endif
+  );
+
   html_source->AddString("cookiesSettingsHelpCenterURL",
                          chrome::kCookiesSettingsHelpCenterURL);
 
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -421,6 +421,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kFocusSpellcheckEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kFocusUpdateFetchingEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kFocusServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kFocusServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -10,6 +10,7 @@
 #include "base/numerics/safe_conversions.h"
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
+#include "chrome/browser/buildflags.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/upgrade_detector/upgrade_detector.h"
@@ -233,6 +234,12 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterStringPref(prefs::kFocusServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kFocusDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kFocusServicesConsented, false);
+#if BUILDFLAG(IS_WIN)
+    registry->RegisterBooleanPref(prefs::kFocusUpdateFetchingEnabled,
+                                  BUILDFLAG(ENABLE_UPDATE_NOTIFICATIONS));
+#else
+    registry->RegisterBooleanPref(prefs::kFocusUpdateFetchingEnabled, false);
+#endif
   }
 
   registry->RegisterBooleanPref(
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -17,6 +17,12 @@
     <message name="IDS_SETTINGS_MORE_ACTIONS_BUTTON_ARIA_LABEL" desc="Aria (accessibility) label for a button that shows a menu with more actions when clicked or tapped">
       More actions for <ph name="BUTTON_TITLE">$1</ph>
     </message>
+    <message name="IDS_SETTINGS_FOCUS_BROWSER_UPDATES" desc="Label for the Focus Browser automatic updates toggle">
+      Automatically update Focus Browser
+    </message>
+    <message name="IDS_SETTINGS_FOCUS_BROWSER_UPDATES_DESCRIPTION" desc="Description for the Focus Browser automatic updates toggle">
+      Check for signed browser updates and install them automatically
+    </message>
     <message name="IDS_SETTINGS_OPENS_IN_NEW_TAB" desc="ARIA (accessibility) label describing a link which opens in a new tab.">
       Opens in new tab
     </message>
