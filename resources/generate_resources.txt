# Windows ICO files are rendered frame-by-frame by
# build_support/generate_focus_browser_icons.py. Do not resize a single large
# PNG here: doing so makes the light/dark contrast keyline disappear at 16px.
